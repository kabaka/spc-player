[package]
name = "spc-apu-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
snes-apu-spcp = { path = "../../vendor/snes-apu-spcp" }
spc-spcp = { path = "../../vendor/spc-spcp" }
